# ===============================
# Ejemplo de configuración .env
# ===============================

# --- Clave de la API de OpenAI (si usas integración con IA)
OPENAI_API_KEY=sk-pon-aqui-tu-clave-de-openai

# --- Clave secreta para firmar JWT y sesiones
SECRET_KEY=tu_clave_secreta_ultra_segura

# --- Algoritmo de encriptación para JWT (normalmente 'HS256')
ALGORITHM=HS256

# --- Tiempo de expiración de los tokens JWT (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Credenciales para la base de datos Postgres
POSTGRES_USER=user
POSTGRES_PASSWORD=tu_password_db
POSTGRES_DB=nombre_base_de_datos

# --- URL completa para conectar a la base de datos (formato: postgresql://usuario:contraseña@host:puerto/base_de_datos)
DATABASE_URL=postgresql://user:tu_password_db@db:5432/nombre_base_de_datos

# ================================================
# Notas importantes:
# - No subas nunca tu .env real al repositorio.
# - Usa este archivo como plantilla y referencia.
# - Cambia los valores de ejemplo por los reales en tu entorno local, de staging o producción.
# ================================================
